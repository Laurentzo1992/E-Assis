def chunk_document(full_text, sommaire_json):
    # À implémenter : découper full_text en chunks selon pages/sections du sommaire
    # Pour l’instant, on retourne le texte complet en un seul chunk
    return [full_text]
